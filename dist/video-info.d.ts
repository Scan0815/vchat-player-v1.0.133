export interface VideoInfo {
    /** muted status of the video element */
    muted: boolean;
    /** paused status of the video element */
    paused: boolean;
    /** a float value between 0 and 1, the volume of the video element */
    volume: number;
}
